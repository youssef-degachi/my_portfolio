// next.config.js
module.exports = {
  // Optional: Add basePath if using a sub-path on GitHub Pages
  basePath: '/my_prortfolio',
  assetPrefix: '/my_prortfolio',
  trailingSlash: true,  // Ensure trailing slash is added to the URLs
};
