SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

EMAIL_USER=yoursecondaryemail@gmail.com
EMAIL_PASS=yourpassword
PRIMARY_EMAIL=yourprimaryemail@gmail.com 


// the EMAIL_USER will send to PRIMARY_EMAIL

# --- Knowledge section (optional) -------------------------------------------
# Neon Postgres connection string of the READ-ONLY role `portfolio_reader`
# (RLS only exposes published entries) — safe to ship in the bundle.
# Created in my-personal-info/db/create-reader.sql; see my-personal-info/docs/SETUP-NEON.md.
# Without it the /knowledge page shows the built-in demo entries.
VITE_DATABASE_URL=
